[pytest]
pythonpath = .
testpaths = tests
python_files = test_*.py
addopts = -v --html=reports/reporte.html --self-contained-html --log-cli-level=INFO
markers =
    ui: pruebas de interfaz con Selenium
    api: pruebas de API con Requests
    negativo: escenarios de error o validación
